[package]
name = "neat_core_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "neat_core_rs"
crate-type = ["rlib"]  # Pure Rust library only

[dependencies]
rand = "0.8"
rayon = "1.10"
fnv = "1.0"
serde = { version = "1.0", features = ["derive"] }
